<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds may talk to a development server over plain HTTP on the local network. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
